# File Indexer — Progress

Трекер прогресса учебного pet-проекта по многопоточности в Java.

## Текущий статус

**Сейчас:** базовый обход директории уже работает.

- [ ] Получение пути от пользователя
- [ ] Рекурсивный обход через `Files.walk()`
- [ ] Фильтрация обычных файлов через `Files.isRegularFile`
- [ ] Фильтрация файлов по расширению
- [ ] Вывод найденных `Path`
- [ ] Преобразование `Path` в `FileInfo`

**Следующий шаг:** создать `FileInfo` и через `Stream.map()` преобразовывать найденные файлы из `Path` в `FileInfo`.

---

## Этап 1 — Базовый файловый индексатор

Цель: сделать рабочую однопоточную версию до добавления многопоточности.

- [ ] Создать проект
- [ ] Получить корневую директорию
- [ ] Обойти директорию через `Files.walk()`
- [ ] Отфильтровать обычные файлы
- [ ] Отфильтровать нужные расширения
- [ ] Создать класс `FileInfo`
- [ ] Сохранять в `FileInfo` путь
- [ ] Получать размер файла через `Files.size()`
- [ ] Получать время последнего изменения
- [ ] Преобразовать `Stream<Path>` в `List<FileInfo>` через `map()`
- [ ] Вывести сведения о найденных файлах

---

## Этап 2 — Producer / Consumer + BlockingQueue

Цель: разделить обнаружение файлов и их обработку между потоками.

- [ ] Создать `FileTask`
- [ ] Создать `BlockingQueue<FileTask>`
- [ ] Реализовать Producer
- [ ] Producer помещает задачи через `put()`
- [ ] Создать несколько Worker-потоков
- [ ] Workers получают задачи через `take()`
- [ ] Проверить работу нескольких потоков
- [ ] Проверить поведение при заполненной очереди

---

## Этап 3 — Потокобезопасный индекс

Цель: безопасно хранить результаты параллельной обработки.

- [ ] Добавить `ConcurrentHashMap<Path, FileInfo>`
- [ ] Workers записывают обработанные файлы в индекс
- [ ] Проверить одновременную запись нескольких потоков
- [ ] Понять, почему здесь нужен `ConcurrentHashMap`, а не `HashMap`

---

## Этап 4 — Атомарная статистика

Цель: собирать статистику без общего большого `synchronized`.

- [ ] `AtomicInteger` для количества обработанных файлов
- [ ] `AtomicInteger` для количества ошибок
- [ ] `LongAdder` для общего количества обработанных байт
- [ ] Вывод текущей статистики
- [ ] Понять разницу между `AtomicInteger` и `LongAdder`

---

## Этап 5 — Interrupt и корректная остановка

Цель: научиться правильно завершать многопоточное приложение.

- [ ] Остановка Producer
- [ ] Использование `interrupt()`
- [ ] Обработка `InterruptedException`
- [ ] Восстановление interrupt-флага там, где это требуется
- [ ] Завершение Worker-потоков
- [ ] Использование `join()`
- [ ] Реализовать вариант с STOP-задачами
- [ ] Проверить завершение при пустой и заполненной очереди

---

## Этап 6 — WatchService

Цель: автоматически реагировать на изменения директории.

- [ ] Подключить `WatchService`
- [ ] Обрабатывать `ENTRY_CREATE`
- [ ] Обрабатывать `ENTRY_MODIFY`
- [ ] Обрабатывать `ENTRY_DELETE`
- [ ] Создавать `FileTask` по событию
- [ ] Обновлять индекс после изменения файла
- [ ] Удалять файл из индекса после удаления

---

## Этап 7 — ThreadPoolExecutor

Цель: заменить ручное управление Worker-потоками стандартным пулом.

- [ ] Создать `ThreadPoolExecutor` вручную
- [ ] Настроить `corePoolSize`
- [ ] Настроить `maximumPoolSize`
- [ ] Настроить `keepAliveTime`
- [ ] Выбрать рабочую очередь
- [ ] Добавить собственный `ThreadFactory`
- [ ] Проверить создание дополнительных потоков
- [ ] Проверить освобождение дополнительных потоков

---

## Этап 8 — Rejection Policy

Цель: понять, что происходит при перегрузке пула.

- [ ] Проверить `AbortPolicy`
- [ ] Проверить `CallerRunsPolicy`
- [ ] Проверить `DiscardPolicy`
- [ ] Проверить `DiscardOldestPolicy`
- [ ] Выбрать политику для проекта
- [ ] Объяснить выбор в README

---

## Этап 9 — Future

Цель: добавить отдельные задачи, результат которых нужен пользователю.

Идея: команда поиска текста по индексированным файлам.

- [ ] Добавить `SearchTask`
- [ ] Отправлять задачу через `submit()`
- [ ] Получать результат через `Future.get()`
- [ ] Добавить отмену через `cancel(true)`
- [ ] Корректно реагировать на interrupt внутри поиска
- [ ] Проверить `isDone()`
- [ ] Проверить `isCancelled()`

---

## Этап 10 — Pause / Resume

Цель: применить `ReentrantLock` и `Condition` в реальном сценарии.

- [ ] Добавить состояние `RUNNING`
- [ ] Добавить состояние `PAUSED`
- [ ] Добавить состояние `STOPPED`
- [ ] Реализовать `pause()`
- [ ] Workers ожидают через `Condition.await()`
- [ ] Реализовать `resume()`
- [ ] Пробуждать через `signalAll()`
- [ ] Проверить корректность при shutdown во время pause

---

## Этап 11 — ForkJoinPool

Цель: использовать Fork/Join для отдельной вычислительной задачи.

Команда `stats` должна считать:

- [ ] Общее количество файлов
- [ ] Общий размер
- [ ] Количество `.java`
- [ ] Количество `.txt` / `.md`
- [ ] Самый большой файл
- [ ] Средний размер файла
- [ ] Создать `RecursiveTask<DirectoryStatistics>`
- [ ] Использовать `fork()`
- [ ] Использовать `compute()`
- [ ] Использовать `join()`
- [ ] Настроить `THRESHOLD`
- [ ] Посмотреть распределение работы между worker-потоками

---

## Этап 12 — Graceful Shutdown

Цель: единообразно и безопасно завершать все компоненты.

- [ ] Перестать принимать новые события
- [ ] Остановить `WatchService`
- [ ] Вызвать `shutdown()`
- [ ] Использовать `awaitTermination()`
- [ ] При необходимости вызвать `shutdownNow()`
- [ ] Закрыть файловые ресурсы
- [ ] Проверить interrupt
- [ ] Проверить завершение при активных задачах
- [ ] Проверить завершение при пустой очереди

---

## Этап 13 — Финализация

- [ ] Разделить код по пакетам
- [ ] Убрать дублирование
- [ ] Добавить обработку ошибок
- [ ] Добавить unit-тесты
- [ ] Добавить concurrency-тесты
- [ ] Создать README
- [ ] Описать архитектуру
- [ ] Добавить схему Producer → Queue → Workers
- [ ] Описать используемые механизмы многопоточности
- [ ] Добавить примеры запуска
- [ ] Залить проект на GitHub

---

## Изученные темы многопоточности

### Основы

- [x] `Thread`
- [x] `Runnable`
- [x] `synchronized`
- [x] Монитор объекта
- [x] Happens-before / основы JMM
- [x] `volatile`

### Locks

- [x] `ReentrantLock`
- [x] `Condition`
- [x] `await()`
- [x] `signal()` / `signalAll()`
- [x] Fair / unfair lock
- [x] `lockInterruptibly()`

### Interrupt

- [x] `interrupt()`
- [x] `isInterrupted()`
- [x] `InterruptedException`
- [x] Восстановление interrupt-флага

### Синхронизаторы и коллекции

- [x] `Semaphore`
- [x] `CountDownLatch`
- [x] `BlockingQueue`
- [x] `ConcurrentHashMap`
- [x] `CopyOnWriteArrayList`
- [x] Atomic-классы

### Пулы и задачи

- [x] Собственный простой ThreadPool
- [x] `ExecutorService`
- [x] `ThreadPoolExecutor`
- [x] `Future`
- [x] Rejection policies
- [x] `ForkJoinPool`
- [x] `RecursiveTask`
- [x] `RecursiveAction`
- [x] `fork()`
- [x] `join()`
- [x] `invokeAll()`
- [x] Work stealing
- [x] Deque в `ForkJoinPool`
- [x] Базовое понимание `managedBlock()`

### Далее

- [ ] Virtual Threads
- [ ] `CompletableFuture`

---

## Заметки / проблемы

Записывай сюда вопросы, баги и решения по ходу разработки.

### Формат

```text
Дата:
Проблема:
Причина:
Решение:
Что понял:
```

### Пример

```text
Проблема:
При фильтрации Stream не находились .md файлы.

Причина:
В lambda проверялся исходный `path`, а не текущий элемент Stream.

Было:
path.toString().endsWith(".md")

Стало:
file.toString().endsWith(".md")

Что понял:
В `filter()` нужно работать с параметром lambda — текущим элементом Stream.
```

---

## Последнее обновление

**22 августа 2026**

Сейчас работает рекурсивный обход директории через `Files.walk()` с фильтрацией файлов.  
Следующая задача: создать `FileInfo` и сделать преобразование `Path -> FileInfo` через `Stream.map()`.
